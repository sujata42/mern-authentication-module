const Home = () => {
  return (
    <div className="page">
      <h1>Welcome to AuthApp</h1>
      <p>
        A simple authentication system built using the MERN stack with secure
        login, registration, and profile management.
      </p>
    </div>
  );
};

export default Home;
